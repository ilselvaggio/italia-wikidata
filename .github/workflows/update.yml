name: Update Map Data Online

on:
  push:
    paths:
      - 'osm/**'
      - 'update.py'
      - 'regions.json'
      - 'index.html'
      - '.github/workflows/update.yml'
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Libraries
        run: pip install requests

      - name: Update and Sync
        run: |
          # 1. Config
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 2. Pull first to get your manual uploads (prevents conflicts)
          git pull origin main --no-rebase
          
          # 3. Run Script (generates data)
          python -u update.py
          
          # 4. Add generated files
          git add osm/*.json data_overpass/*.geojson metadata.json
          
          # 5. Commit & Push
          git commit -m "Auto-update map data [skip ci]" || echo "No changes to commit"
          git push origin main
